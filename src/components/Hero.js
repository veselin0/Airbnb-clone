const Hero = () => {
    return (
        <section className="hero">
            <img
                className="group77"
                src="../images/photo-grid.png"
                alt="some views of Nature"
            />
            <h1 className="title">Online Experiences</h1>
            <p className="text">
                Join unique interactive activities led by one-of-a-kind
                hosts—all without leaving home.
            </p>
        </section>
    );
};

export default Hero;
